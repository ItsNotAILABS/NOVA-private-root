# NOVA AI Builder Readme (Primary)

Classification: `BUILDER_CONFIDENTIAL`

Audience:
- AI builders only (internal family-operated AI teams).

This file is intentionally optimized for machine agents, not public users.

---

## 1) Build Doctrine in one block

The system is a sovereign organism platform.

- Absolutes define reality constraints.
- Laws define allowed transformation.
- Canonical models define shared language.
- Core A defines runtime truth.
- Core B defines industrial build execution.
- Products are controlled projections under zero-exposure policy.

No collapse allowed between:
- Absolute, Law, Model, Engine, Core, Module, Lab, Workforce, Product.

---

## 2) Required read order for AI builders

1. `docs/NOVA_BUILD_CONSTITUTION.md`
2. `docs/NOVA_FULL_SPHERE_ARCHITECTURE.md`
3. `docs/NOVA_ACCESS_TIERS_AND_READING_POLICY.md`
4. `docs/NOVA_REUSE_KIT.md`
5. `docs/NOVA_RESONANCE_ARTIFACT_SYSTEM.md`
6. `docs/templates/NOVA_DOCTRINE_PACK.yaml`
7. `docs/templates/NOVA_LAW_REGISTRY.yaml`
8. `docs/templates/NOVA_TRANSFER_PROTOCOL.yaml`
9. `docs/templates/NOVA_BUILD_INSTANCE_TEMPLATE.yaml`

Do not start implementation until these are loaded and aligned.

---

## 3) Mandatory gates before changes

Gate A (Core A readiness) must hold before enabling Core B writes.

Gate B (workforce activation) must hold before industrial workflows.

Gate C (public projection) must hold before any external exposure.

If any gate fails:
- stop,
- emit failure artifact,
- route to rollback/remediation.

---

## 4) Builder contract

AI builders must:
- preserve doctrine and model parity,
- keep backend/frontend contract parity,
- retain no-drop continuity and replay traceability,
- never leak doctrine internals to projection surfaces.

AI builders must not:
- rewrite doctrine categories without governance process,
- bypass Core A acceptance boundaries,
- expose sacred/private clauses in public output.

---

## 5) Output requirements per change

Every meaningful change must produce:
- architecture delta note,
- policy impact note,
- replay/evidence pointer,
- exposure safety check result.

If output is missing any of these, change is incomplete.

---

## 6) Family workspace pointer

Family-facing reading workspace:
- `docs/family-library/README_FAMILY_LIBRARY.md`

That folder is the compact human memory library.
All other docs are AI-builder-targeted by default.
