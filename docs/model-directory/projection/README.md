# Projection Model Family

Classification: `PUBLIC_SAFE`

Purpose:
- define external-safe model contracts for product/API surfaces,
- enforce zero-exposure boundaries for all projection payloads,
- and maintain strict separation from runtime/internal sovereign models.

Primary owner:
- product-authority

Primary consumers:
- external-operator-stack
- product-api-gateway

Required gates:
- GATE-C

Canonical model list:
- `docs/model-directory/projection/MODELS.yaml`
